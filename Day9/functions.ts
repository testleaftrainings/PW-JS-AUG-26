function add(a:number,b:number):number{
let c = a+b
//console.log(c);
return c
}

console.log(add(12, 54))

/* 
Function in TS is a reusable block of code used to perform a specific task
TS allows us to define the data types of parameters and the return value,
which provides type safety and helps catch errors during compilation */
